rest:
  # Add credentials for other channels here
  # More details: https://rasa.com/docs/rasa/messaging-and-voice-channels

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true